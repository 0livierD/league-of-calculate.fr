{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container-gauche ombre">
        <div class="flex">
            <div>
                vfdq
            </div>
            <div class="bordure">
                <button class="button"><a href="{{ path('app_register') }}">S'inscrire</a></button>
                <button class="button"><a href="{{ path('app_login') }}">Se connecter</a></button>
            </div>
        </div>
    </div>
    <div class="container-droite ombre">

    </div>
{% endblock %}
